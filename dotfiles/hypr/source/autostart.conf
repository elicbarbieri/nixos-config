

# Wallpaper daemon (required by ax-shell)
exec-once = swww-daemon

# Lock screen on boot (after wallpaper daemon initializes)
exec-once = sleep 0.5 && hyprlock

# Ax-Shell
exec-once = uwsm app -- ax-shell
