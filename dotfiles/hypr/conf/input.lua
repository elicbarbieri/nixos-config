-- =============================================================================
-- Input: keyboard / mouse / touchpad
-- =============================================================================

hl.config({
    input = {
        follow_mouse       = 1,
        sensitivity        = 0,
        repeat_rate        = 25,
        repeat_delay       = 600,
        numlock_by_default = true,

        touchpad = {
            natural_scroll = true,
            scroll_factor  = 0.25,
        },
    },
})
